'use client';

/**
 * Viewshed analysis tool component
 * Progressive rendering of viewshed with accurate occlusion
 * Note: Hook is called in ViewshedPanelContent, not here
 */
export const ViewshedTool = () => {
    return null;
};
